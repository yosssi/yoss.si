package consts

const (
	ConfigDir                       string = "./config"
	ServerJsonPath                  string = ConfigDir + "/server.json"
	LoggerJsonPath                  string = ConfigDir + "/logger.json"
	ErrorMessageInternalServerError string = "An internal server error occurred."
)
